/**
 * Created by ylc on 2017/6/8.
 */
// import Vue from 'vue'

(function(doc, win){
  win.getData = '';
  showInfoFromJava = function(){
    console.log('ssss')
    // JsObject.showToast("------"+result)
    getData = true;

    // var data = require('./demo.js');
    // console.log(data.default.aa);
    // Vue.prototype.getData = true
  };
/*if (!doc.addEventListener) return;
  win.addEventListener(showInfoFromJava, false);
  doc.addEventListener(showInfoFromJava, false);*/

})(document, window);



